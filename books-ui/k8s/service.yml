apiVersion: v1
kind: Service
metadata:
  name: books-ui
  labels:
    app: books-ui
spec:
  type: ClusterIP
  selector:
    app: books-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
